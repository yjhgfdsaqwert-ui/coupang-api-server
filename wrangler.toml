name = "coupang-api-server"
main = "worker.js"
compatibility_date = "2026-09-11"

[observability.logs]
enabled = true
invocation_logs = true

# 기존 DISCORD_BOT Durable Object 설정은
# 현재 사용 중인 설정을 그대로 유지해야 함.
